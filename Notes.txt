Editeur

Boutons sur les côtés

AJAX pour réccupérer le texte dans la div de l'éditeur y concaténer le block puis le retrouner.

Menus dépliants pour les blocks.


	if(isset($_GET['post_to_delete'])){
		$action = 'delete';
		$post_to_delete = $_GET['post_to_delete'];
		delete_post($post_to_delete);
		switch($type){
			case 'post':
				header:('location: post.php');
				break;
			case 'page':
				header:('location: page.php');
				break;
		}
	}